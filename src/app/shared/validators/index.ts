import { minSelectedOptions } from './min-selected-options';
import { requiredTrimmed } from './required-trimmed.validator';

export const CustomValidators = {
  requiredTrimmed,
  minSelectedOptions,
};
